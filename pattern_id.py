s = str(input()) #Converts value to string
l = str(input())

x = s.find(l) #Finds the first occurance of the value/string
print(x)
y = len(l) #Used to calculate the length 
range = x + y #Calculates the 

print(range) #Prints the range
print(x , 'to' , range) #Prints the final output
